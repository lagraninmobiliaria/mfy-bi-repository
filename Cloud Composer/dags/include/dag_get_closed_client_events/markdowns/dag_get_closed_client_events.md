## Get closed client events
### Description
This DAGs fetches all the closed client events created during the last day and stores them in a BigQuery table.
In case the table doesn't exists, it is created.